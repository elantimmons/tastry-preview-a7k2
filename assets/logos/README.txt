Client logos for the Wine Origin page logo wall.

Drop each approved client's logo file here, then (in wine.html) replace the
<span class="name">…</span> inside that client's .cell with an <img>, e.g.:

  <div class="cell"><img src="assets/logos/patz-hall.svg" alt="Patz & Hall" /></div>

Preferred format: SVG (single color works best with the warm palette), or a
transparent PNG at 2x. Target display height is ~46px.

Expected filenames (match the comments in wine.html):
  patz-hall.svg
  ste-michelle.svg
  oneill.svg
  phase-2-cellars.svg
  rndc.svg

Add more cells for any additional approved clients.
